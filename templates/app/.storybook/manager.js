import { addons } from '@storybook/addons';

addons.setConfig({
  // panelPosition: 'right',
  toolbar: {
    title: { hidden: true, },
    zoom: { hidden: true, },
    copy: { hidden: true, },
  },
});
